
name: Push To Yandex Cloud

on:
  push:
    branches: [ main ]

  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
      - name: YandexCloud CLI version
        run: yc version

